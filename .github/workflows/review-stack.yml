name: Review Stack
# review-stack-control: post-rollout verification against current v1 baseline

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  review-stack:
    uses: madmoneymike5/review-stack-runtime/.github/workflows/review-stack.yml@v1
    with:
      native-command: >-
        npm ci --ignore-scripts && npm run typecheck
      central-ref: v1
